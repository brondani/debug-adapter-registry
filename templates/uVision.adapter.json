{
    "tasks": [{
        "label": "CMSIS Load+Run",
        "type": "shell",
        "command": "<%= uv4 %>",
        "options": { "cwd": "${workspaceFolder}/<%= solution_folder %>" },
        "args": [
            "<%= solution_path %>",
            "<% for (const arg of args) { %>",
            "<%= arg %>\n",
            "<% } %>"
        ],
        "problemMatcher": []
    }]
}
